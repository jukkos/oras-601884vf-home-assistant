# ESPHome configuration

The exact production YAML is intentionally **not reconstructed from memory**.

Next step:

1. copy the current working YAML from the real project
2. replace Wi-Fi/API/OTA credentials and site-specific values with `!secret` references
3. compile/validate the sanitized version
4. compare behavior with the running device before treating it as the repository source of truth
